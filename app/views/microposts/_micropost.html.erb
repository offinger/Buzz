<tr>
	<td class="micropost">
		<span class="content"><%= wrap auto_link(micropost.content, :urls) %></span><!--wrap je micropost helper koji sluzi da smanji reci koje su prevelike i izlaze iz okvira predvidjenog za micropost -->
		<span class="timestamp">
			Posted <%= time_ago_in_words(micropost.created_at) %> ago.
		</span>
	</td>
	<% if current_user?(micropost.user) %>
	<td>
		<%= link_to "delete", micropost, :method => :delete,
										 :confirm => "You sure?",
										 :title => micropost.content %>
	</td>
	<% end %>
</tr>